import {StyleProp, ViewStyle} from "react-native"

const style = {
  outer: <StyleProp<ViewStyle>>{
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center'
  }
}

export default style
